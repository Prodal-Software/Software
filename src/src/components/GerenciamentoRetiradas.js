import { Box, Typography, Grid } from "@mui/material";


export const GerenciamentoRetiradas = () => {
  return (
    <Grid container>
      <Grid item columns={12}>
        <Box px={'10em'} bgcolor={'divider'}>
          <Typography>Hello</Typography>
        </Box>
      </Grid>
    </Grid>
  );
};
export default GerenciamentoRetiradas;
